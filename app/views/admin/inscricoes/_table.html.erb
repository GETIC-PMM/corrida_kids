<%- model_class=Inscricao -%>

<% remote ||= remote %>
<% show_collapse ||= show_collapse %>


<div class="accordion" id="accordionInscricao">
  <div class="accordion-item">
    <h2 class="accordion-header">
      <button class="accordion-button <%= show_collapse.blank? ? 'collapsed' : '' %>" type="button" data-bs-toggle="collapse" data-bs-target="#inscricao" aria-expanded="<%= show_collapse.present? %>" aria-controls="Inscricao">
        <%= model_class.model_name.human(count: 2).titleize %>
      </button>
    </h2>
    <div id="inscricao" class="accordion-collapse collapse <%= show_collapse %>" data-bs-parent="#accordionInscricao">
      <div class="accordion-body">
        <%= render partial: "admin/inscricoes/datatable" , locals: {remote: remote} %>
      </div>
    </div>
  </div>
</div>